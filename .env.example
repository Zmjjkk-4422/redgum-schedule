# Copy this file to `.env` and adjust as needed. Never commit the real `.env`.
# Flask configuration profile: dev | test | production
FLASK_CONFIG=dev

# Generate a real secret for anything beyond local development.
SECRET_KEY=change-me-for-production-use

# SQLite database file (relative paths are resolved from the repository root)
DATABASE=instance/redgum.db
